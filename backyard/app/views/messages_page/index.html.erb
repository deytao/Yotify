<h1>Messages</h1>
<a href="<%= url_for controller: "messages_page", action: "new" %>" class="ui button blue">Create</a>
<table class="ui celled table">
  <thead>
    <tr>
      <th>Subject</th>
      <th>Content</th>
      <th>Created</th>
      <th>Created by</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @messages.each do |message| %>
      <tr>
        <td><%= message["subject"] %></td>
        <td><%= message["body"] %></td>
        <td><%= message["created_at"] %></td>
        <td><%= message["user_id"] %></td>
        <td>
          <a href="<%= url_for controller: "messages_page", action: "create" %>" class="ui button blue mini">Details</a>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
